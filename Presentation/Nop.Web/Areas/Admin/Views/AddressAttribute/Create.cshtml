@model AddressAttributeModel
@{
    //page title
    ViewBag.PageTitle = T("Admin.Address.AddressAttributes.AddNew").Text;
    //active menu item (system name)
    NopHtml.SetActiveMenuItemSystemName("Customer and user settings");
}

<form asp-controller="AddressAttribute" asp-action="Create" method="post" id="addressattribute-form">
    <div class="content-header clearfix">
        <h1 class="float-left">
            @T("Admin.Address.AddressAttributes.AddNew")
            <small>
                <em class="fas fa-arrow-circle-left"></em>
                <a asp-action="List">@T("Admin.Address.AddressAttributes.BackToList")</a>
            </small>
        </h1>
        <div class="float-right">
            <button type="submit" name="save" class="btn btn-primary">
                <em class="far fa-save"></em>
                @T("Admin.Common.Save")
            </button>
            <button type="submit" name="save-continue" class="btn btn-primary">
                <em class="far fa-save"></em>
                @T("Admin.Common.SaveContinue")
            </button>
            @await Component.InvokeAsync("AdminWidget", new { widgetZone = AdminWidgetZones.AddressAttributeDetailsButtons, additionalData = Model })
        </div>
    </div>
    @await Html.PartialAsync("_CreateOrUpdate", Model)
</form>